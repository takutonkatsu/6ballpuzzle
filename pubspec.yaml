name: six_ball_puzzle
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter
  flame: ^1.16.0
  firebase_core: ^4.7.0
  firebase_auth: ^6.1.0
  firebase_database: ^12.3.0
  shared_preferences: ^2.3.3
  audioplayers: ^6.6.0
  flame_audio: ^2.12.1
  google_mobile_ads: ^8.0.0
  firebase_app_check: ^0.4.3
  in_app_purchase: ^3.2.3
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/rl_model.json
    - assets/audio/
    - assets/images/
